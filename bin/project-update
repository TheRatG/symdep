#!/usr/bin/env bash

REAL_PATH="$(cd "$(dirname "$0")" && pwd -P)";
DIR=`dirname "${REAL_PATH}"`;
BIN_TARGET="$DIR/symdep";

SRC_DIR="`pwd`"
cd "$SRC_DIR"
"$BIN_TARGET" --build-type="dev" deploy local "$@"
